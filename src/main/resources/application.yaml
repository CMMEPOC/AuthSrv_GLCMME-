spring:
  application:
    name: authentication
  datasource:
    url: jdbc:postgresql://localhost:5432/fundtransfer_db
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
jwt:
  # HS512 requires a secret of at least 64 bytes. Override this in real environments.
  secret: ${JWT_SECRET:AWOum5UmQdECMaapOD/HiEZfCMbKZG0EYQ0Vd7rwUIS8JWbrdCmBRbvaRKSPziPLAYv/6w3eN/7opl4xw89Iyw==}
  expiration-ms: ${JWT_EXPIRATION_MS:3600000}

